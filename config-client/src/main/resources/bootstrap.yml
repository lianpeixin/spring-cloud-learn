spring:
  cloud:
    config:
      profile: test
#      uri: http://127.0.0.1:8087/
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
